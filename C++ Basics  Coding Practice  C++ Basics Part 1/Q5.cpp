#include <bits/stdc++.h>
using namespace std;

int main() {
    
    long long num;
    int divisor;
    
    cin >> num >> divisor;
    
    cout << fixed << setprecision(2) << (double)num / divisor << '\n';

    return 0;
}